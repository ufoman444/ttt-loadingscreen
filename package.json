{
  "name": "ttt-loadingscreen",
  "version": "1.0.0",
  "private": true,
  "description": "Ladebildschirm fuer Garry's Mod / Trouble in Terrorist Town, eingebunden ueber sv_loadingurl",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test \"tests/*.test.mjs\"",
    "dev": "node scripts/dev-server.mjs",
    "maps:fetch": "node scripts/fetch-workshop-maps.mjs",
    "maps:check": "node scripts/check-workshop-maps.mjs"
  }
}
